# Relayer Service Configuration

# Port to run the relayer service on
PORT=8787

# Helius RPC endpoint with API key (supports ZK Compression)
HELIUS_RPC_URL=https://devnet.helius-rpc.com?api-key=YOUR_HELIUS_API_KEY

# Fee payer keypair (base58 encoded secret key) - this wallet pays transaction fees
# Generate with: solana-keygen new --no-bip39-passphrase --outfile relayer-keypair.json
# Then encode: cat relayer-keypair.json | jq -r '.[0:32] | map(tostring) | join(",")' | base58
FEE_PAYER_SECRET_KEY=YOUR_BASE58_SECRET_KEY_HERE

# Shield program ID (deployed on devnet)
SHIELD_PROGRAM_ID=3KN2qrmEtPyk9WGu9jJSzLerxU8AUXAy8Dp6bqw5APDz

# CORS allowed origins (comma-separated)
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:4173

# Log level (debug, info, warn, error)
LOG_LEVEL=info

# =============================================
# SWAP CONFIGURATION
# =============================================

# NOC token price in USD (fixed price - not on exchanges yet)
# Update this as the token price changes, or integrate with a price oracle
NOC_PRICE_USD=0.30

# Swap fee in basis points (100 = 1%, 12 = 0.12%)
SWAP_FEE_BPS=12
